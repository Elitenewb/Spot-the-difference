# AI bridge background communication investigation

## Completion gate

- Deterministic full-message-path test covers worker memory loss and returns 10 regions to Creator.
- Full automated suite and syntax checks pass.
- Two consecutive installed-extension runs advance beyond 10% with ChatGPT left in the background and no user interaction.

## Attempts and evidence

1. Extension 0.3.13 replaced assistant-message counts with completed JSON signatures. Live ChatGPT contained valid 10-region JSON, but Creator still timed out at 10%; parser-only hypothesis rejected.
2. Extension 0.3.14 persisted active-job routing in `chrome.storage.session`. A worker-memory-loss unit test passed, but a second live run still hung at 10%; routing-persistence-only hypothesis rejected.
3. Current live evidence: Creator remains at 10%; ChatGPT has one complete assistant turn containing exactly 10 region objects; Stop is absent; ChatGPT document reports visible; no Creator console error. This rules out incomplete JSON and background-page visibility as the direct cause.
4. Next action: add durable, non-sensitive event tracing across adapter result, worker matching, and Creator forwarding; expose the trace and installed version through the bridge ping; reproduce with the saved live test image without asking the user to test.
5. History comparison isolated the transport regression in `f5dda88`: the previously seamless adapter kept the `tabs.sendMessage` response open until parsing completed, while `f5dda88` acknowledged immediately and attempted a second independent result event later. Restored the delayed response channel in 0.3.16 while retaining signature parsing and durable diagnostics. The deterministic full-path test now clears worker memory during the delayed adapter response and still delivers 10 regions to Creator.
6. Live run 1 on installed extension 0.3.16 used the hosted Creator debug fixture with ChatGPT left in the background. Started 2026-07-16T23:17:04.988Z; adapter trace recorded `submitted` at 23:17:10.436Z, `parsed` at 23:18:31.775Z, and successful `SPOT_DIFF_ANALYSIS_RESULT` forwarding at 23:18:31.820Z. Creator showed 10 region prompts, advanced to 27%, and began the edit queue. The first edit also returned successfully before the queue was cancelled. This is a passing live handoff.
7. Live run 2 began at 2026-07-16T23:22:03.241Z with the same synthetic fixture and reached 10% after submission. The exact background ChatGPT tab contained one complete, valid array of 10 regions, but the adapter timed out. Direct DOM inspection confirmed the assistant selector, conversation-turn selector, absent Stop control, and JSON parser all recognized the finished response after the timeout. This rejects the delayed-response transport as the only cause.
8. Two post-original completion gates could reject already-complete JSON: background throttling could prevent observer/interval delivery until the timeout callback, and `waitForAnalysis` refused structurally complete JSON while ChatGPT still exposed a Stop control. Version 0.3.17 added a final timeout parse and removed the Stop dependency; regression tests covered both.
9. Live 0.3.17 run started 2026-07-16T23:37:22.920Z and failed at the three-minute boundary. The untouched background ChatGPT tab then contained a structurally complete 10-region response with the expected assistant and conversation-turn selectors and no Stop control. This proves the timer woke before ChatGPT's queued background render, checked too early, and destroyed the observer/polling that would have received the response. Version 0.3.18 removes the wall-clock analysis timeout entirely: the existing mutation observer and fallback polling remain active until valid JSON, an explicit tab/transport failure, or user cancellation. Two consecutive live 0.3.18 runs remain required.
10. Live 0.3.18 run started 2026-07-16T23:44:29.459Z. The extension remained pending beyond its former three-minute boundary, but Creator's separate four-minute watchdog aborted at 23:48:47. The untouched ChatGPT tab again contained a valid 10-region response only after its event loop was externally woken. Version 0.3.19 adds a persisted MV3 alarm probe every 30 seconds: the worker wakes the background content script, gives queued ChatGPT rendering one second to settle, parses the current DOM, and forwards a found result through the stored Creator route without activating or clicking the tab. The Creator analysis and replacement-analysis wall-clock watchdogs were also removed so a valid slow response cannot be discarded; Cancel remains available and the image-edit timeout is unchanged. A deterministic test clears worker memory, fires the probe path, and verifies all 10 regions reach Creator. Two consecutive live 0.3.19 runs remain required.
11. Live 0.3.19 run started 2026-07-16T23:55:59.144Z. Durable traces show probes firing precisely every 30 seconds from 23:56:31 through 00:00:31, but none returned even a pending response; each `tabs.sendMessage` remained open because Chrome had frozen the inactive ChatGPT tab's entire event loop. The hosted Creator's old four-minute watchdog then aborted. This is conclusive evidence that an inactive tab cannot satisfy the background requirement on this Chrome build. Version 0.3.20 keeps ChatGPT active in its own unfocused Chrome window: Creator retains focus, while ChatGPT remains browser-schedulable without user interaction. Alarm probing and persisted routing remain as recovery. Two consecutive live 0.3.20 runs remain required.
12. Live 0.3.20 run 1 started 2026-07-17T00:05:45.791Z with Creator focused and no ChatGPT interaction. The adapter parsed 10 regions at 00:06:53.293Z, the worker received them at 00:06:53.297Z and forwarded `SPOT_DIFF_ANALYSIS_RESULT` at 00:06:53.328Z. Creator showed exactly 10 region prompts, advanced to 18%, and started the edit queue. Pass.
13. Live 0.3.20 run 2 started 2026-07-17T00:08:32.380Z under the same unattended conditions. The adapter parsed 10 regions at 00:09:29.970Z, the worker received them at 00:09:29.989Z and forwarded `SPOT_DIFF_ANALYSIS_RESULT` at 00:09:30.027Z. Creator again showed exactly 10 region prompts, advanced to 18%, and started the edit queue. Pass. The user also visually confirmed the separate unfocused ChatGPT window existed while Creator remained in front.
